{
  "name": "components",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "dev": "npm run clean && parcel src/{*.html,**/*.html} -p 3000",
    "build": "rimraf ./dist && parcel build src/{*,**/*}.html --dist-dir dist",
    "clean": "rimraf ./dist && rimraf ./.parcel-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "2.12.0",
    "parcel": "^2.12.0",
    "sass": "1.72.0",
    "rimraf": "5.0.5"
  }
}
