# CSS only Aceternity UI

This is an attempt to make some of the Aceternity UI components in just css (scss).

Some features I'm using only work in chrome and other chromium based browsers as of now :(
